_schema-version: 3.3.0
ID: zentracore
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: zentracore-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: zentracore-auth
      - name: zentracore-db

  - name: zentracore-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: zentracore-db

resources:
  - name: zentracore-auth
    type: com.sap.xs.uaa
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: zentracore-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'Regional_Admins (zentracore ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Regional_Admins'
  - name: zentracore-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
