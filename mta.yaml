_schema-version: 3.3.0
ID: zcom
description: A simple CAP project.
version: 1.0.0
modules:
  - name: zcom-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: zcom-db
      - name: zcom-auth
      - name: zcom-destination-service
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: zcom-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: zcom-db
    parameters:
      buildpack: nodejs_buildpack
  - name: zcom-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: zcom-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - zcomapp.zip
          name: zcomapp
          target-path: resources/
  - name: zcomapp
    type: html5
    path: app/zcomapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: zcom-destination-content
    type: com.sap.application.content
    requires:
      - name: zcom-destination-service
        parameters:
          content-target: true
      - name: zcom-repo-host
        parameters:
          service-key:
            name: zcom-repo-host-key
      - name: zcom-auth
        parameters:
          service-key:
            name: zcom-auth-key
    parameters:
      content:
        instance:
          destinations:
            - Name: zcomapprouter_zcom_repo_host
              ServiceInstanceName: zcom-html5-service
              ServiceKeyName: zcom-repo-host-key
              sap.cloud.service: zcomapprouter
            - Authentication: OAuth2UserTokenExchange
              Name: zcomapprouter_zcom_auth
              ServiceInstanceName: zcom-auth
              ServiceKeyName: zcom-auth-key
              sap.cloud.service: zcomapprouter
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
resources:
  - name: zcom-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: zcom-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: zcom-html5-service
      service-plan: app-host
  - name: zcom-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
      service: destination
      service-name: zcom-destination-service
      service-plan: lite
  - name: zcom-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: zcom-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
